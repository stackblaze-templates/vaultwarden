# Vaultwarden
Self-hosted Bitwarden-compatible password manager server.
## Local Development
```bash
docker compose up
```
Visit `http://localhost:8080`.
## Deploy on StackBlaze
This template includes a `stackblaze.yaml` that provisions Vaultwarden with PostgreSQL.
